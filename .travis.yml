sudo: false
language: ruby
cache: bundler
before_install:
  - gem update --system
  - gem install bundler
addons:
  apt:
    packages:
      - libgmp-dev

env:
  - USE_MINI_PORTILE=true
rvm:
  - ruby-head
